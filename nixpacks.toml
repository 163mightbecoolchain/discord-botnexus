[project]
name = "main"

[phases.setup]
commands = [
  "python -m pip install --upgrade pip"
]

[phases.install]
commands = [
  "pip install -r requirements.txt"
]

[env]
PYTHONUNBUFFERED = "1"

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python main.py"
